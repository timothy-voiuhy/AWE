# AWE Frontend

React and TypeScript web client for AWE.

```bash
npm install
npm run dev
```

The frontend runs at `http://localhost:4000`. The Vite development server
proxies `/api` to the backend at `http://localhost:8001`.
